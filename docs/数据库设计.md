# 婚礼主持档期管理系统 - 数据库设计文档

## 1. 数据库概述

### 1.1 设计原则
- **规范化设计**：遵循第三范式，减少数据冗余
- **性能优化**：合理设置索引，优化查询性能
- **扩展性**：支持水平扩展和垂直扩展
- **数据完整性**：通过约束保证数据一致性

### 1.2 技术选型
- **主数据库**：MySQL 8.0+
- **缓存数据库**：Redis 6.0+
- **搜索引擎**：Elasticsearch 7.0+
- **文件存储**：阿里云OSS / AWS S3

## 2. 数据表设计

### 2.1 用户管理模块

#### 2.1.1 用户表 (users)
```sql
CREATE TABLE `users` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `password_hash` varchar(255) NOT NULL COMMENT '密码哈希',
  `avatar` varchar(500) DEFAULT NULL COMMENT '头像URL',
  `real_name` varchar(50) DEFAULT NULL COMMENT '真实姓名',
  `gender` tinyint(1) DEFAULT NULL COMMENT '性别：1-男，2-女',
  `birthday` date DEFAULT NULL COMMENT '生日',
  `location` varchar(100) DEFAULT NULL COMMENT '所在地区',
  `bio` text COMMENT '个人简介',
  `user_type` tinyint(1) NOT NULL DEFAULT '1' COMMENT '用户类型：1-个人，2-团队，3-管理员',
  `status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '状态：1-正常，2-禁用，3-待审核',
  `verified` tinyint(1) NOT NULL DEFAULT '0' COMMENT '是否实名认证：0-否，1-是',
  `last_login_at` timestamp NULL DEFAULT NULL COMMENT '最后登录时间',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_email` (`email`),
  UNIQUE KEY `uk_phone` (`phone`),
  KEY `idx_user_type` (`user_type`),
  KEY `idx_status` (`status`),
  KEY `idx_location` (`location`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 2.1.2 用户档案表 (user_profiles)
```sql
CREATE TABLE `user_profiles` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '档案ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `professional_title` varchar(100) DEFAULT NULL COMMENT '职业头衔',
  `experience_years` int(11) DEFAULT NULL COMMENT '从业年限',
  `service_count` int(11) NOT NULL DEFAULT '0' COMMENT '服务次数',
  `rating` decimal(3,2) NOT NULL DEFAULT '0.00' COMMENT '评分',
  `rating_count` int(11) NOT NULL DEFAULT '0' COMMENT '评价数量',
  `price_range_min` decimal(10,2) DEFAULT NULL COMMENT '价格区间最低',
  `price_range_max` decimal(10,2) DEFAULT NULL COMMENT '价格区间最高',
  `service_areas` json DEFAULT NULL COMMENT '服务区域',
  `specialties` json DEFAULT NULL COMMENT '专业技能标签',
  `contact_phone` varchar(20) DEFAULT NULL COMMENT '联系电话',
  `contact_wechat` varchar(50) DEFAULT NULL COMMENT '微信号',
  `contact_qq` varchar(20) DEFAULT NULL COMMENT 'QQ号',
  `business_license` varchar(500) DEFAULT NULL COMMENT '营业执照',
  `certificates` json DEFAULT NULL COMMENT '相关证书',
  `view_count` int(11) NOT NULL DEFAULT '0' COMMENT '访问量',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_rating` (`rating`),
  KEY `idx_price_range` (`price_range_min`, `price_range_max`),
  KEY `idx_service_count` (`service_count`),
  CONSTRAINT `fk_user_profiles_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户档案表';
```

#### 2.1.3 第三方登录表 (user_oauth)
```sql
CREATE TABLE `user_oauth` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `provider` varchar(20) NOT NULL COMMENT '第三方平台：wechat, qq, weibo',
  `provider_id` varchar(100) NOT NULL COMMENT '第三方用户ID',
  `provider_data` json DEFAULT NULL COMMENT '第三方用户数据',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_provider_id` (`provider`, `provider_id`),
  KEY `idx_user_id` (`user_id`),
  CONSTRAINT `fk_user_oauth_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='第三方登录表';
```

### 2.2 团队管理模块

#### 2.2.1 团队表 (teams)
```sql
CREATE TABLE `teams` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '团队ID',
  `name` varchar(100) NOT NULL COMMENT '团队名称',
  `logo` varchar(500) DEFAULT NULL COMMENT '团队Logo',
  `description` text COMMENT '团队描述',
  `owner_id` bigint(20) unsigned NOT NULL COMMENT '团队负责人ID',
  `member_count` int(11) NOT NULL DEFAULT '1' COMMENT '成员数量',
  `status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '状态：1-正常，2-禁用',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_owner_id` (`owner_id`),
  KEY `idx_status` (`status`),
  CONSTRAINT `fk_teams_owner_id` FOREIGN KEY (`owner_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='团队表';
```

#### 2.2.2 团队成员表 (team_members)
```sql
CREATE TABLE `team_members` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `team_id` bigint(20) unsigned NOT NULL COMMENT '团队ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `role` varchar(50) NOT NULL DEFAULT 'member' COMMENT '角色：owner, admin, member',
  `permissions` json DEFAULT NULL COMMENT '权限列表',
  `joined_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '加入时间',
  `status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '状态：1-正常，2-禁用',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_team_user` (`team_id`, `user_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_role` (`role`),
  CONSTRAINT `fk_team_members_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_team_members_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='团队成员表';
```

### 2.3 档期管理模块

#### 2.3.1 档期表 (schedules)
```sql
CREATE TABLE `schedules` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '档期ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `team_id` bigint(20) unsigned DEFAULT NULL COMMENT '团队ID',
  `date` date NOT NULL COMMENT '日期',
  `time_slots` json NOT NULL COMMENT '时间段：[{"start":"09:00","end":"12:00","status":"available"}]',
  `status` varchar(20) NOT NULL DEFAULT 'available' COMMENT '状态：available-可用，booked-已预订，rest-休息',
  `price` decimal(10,2) DEFAULT NULL COMMENT '价格',
  `location` varchar(200) DEFAULT NULL COMMENT '服务地点',
  `notes` text COMMENT '备注',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_date` (`user_id`, `date`),
  KEY `idx_team_id` (`team_id`),
  KEY `idx_date` (`date`),
  KEY `idx_status` (`status`),
  KEY `idx_location` (`location`),
  CONSTRAINT `fk_schedules_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_schedules_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='档期表';
```

#### 2.3.2 预约表 (bookings)
```sql
CREATE TABLE `bookings` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '预约ID',
  `booking_no` varchar(32) NOT NULL COMMENT '预约编号',
  `host_id` bigint(20) unsigned NOT NULL COMMENT '主持人ID',
  `client_id` bigint(20) unsigned NOT NULL COMMENT '客户ID',
  `schedule_id` bigint(20) unsigned NOT NULL COMMENT '档期ID',
  `wedding_date` date NOT NULL COMMENT '婚礼日期',
  `wedding_time` time NOT NULL COMMENT '婚礼时间',
  `wedding_location` varchar(200) NOT NULL COMMENT '婚礼地点',
  `contact_name` varchar(50) NOT NULL COMMENT '联系人姓名',
  `contact_phone` varchar(20) NOT NULL COMMENT '联系人电话',
  `guest_count` int(11) DEFAULT NULL COMMENT '宾客人数',
  `budget` decimal(10,2) DEFAULT NULL COMMENT '预算',
  `requirements` text COMMENT '特殊要求',
  `status` varchar(20) NOT NULL DEFAULT 'pending' COMMENT '状态：pending-待确认，confirmed-已确认，cancelled-已取消，completed-已完成',
  `confirmed_at` timestamp NULL DEFAULT NULL COMMENT '确认时间',
  `cancelled_at` timestamp NULL DEFAULT NULL COMMENT '取消时间',
  `cancel_reason` varchar(500) DEFAULT NULL COMMENT '取消原因',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_booking_no` (`booking_no`),
  KEY `idx_host_id` (`host_id`),
  KEY `idx_client_id` (`client_id`),
  KEY `idx_schedule_id` (`schedule_id`),
  KEY `idx_wedding_date` (`wedding_date`),
  KEY `idx_status` (`status`),
  CONSTRAINT `fk_bookings_host_id` FOREIGN KEY (`host_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_bookings_client_id` FOREIGN KEY (`client_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_bookings_schedule_id` FOREIGN KEY (`schedule_id`) REFERENCES `schedules` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='预约表';
```

### 2.4 内容管理模块

#### 2.4.1 作品表 (works)
```sql
CREATE TABLE `works` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '作品ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `title` varchar(200) NOT NULL COMMENT '作品标题',
  `description` text COMMENT '作品描述',
  `type` varchar(20) NOT NULL COMMENT '作品类型：image-图片，video-视频',
  `cover_url` varchar(500) DEFAULT NULL COMMENT '封面图片URL',
  `media_urls` json NOT NULL COMMENT '媒体文件URL列表',
  `tags` json DEFAULT NULL COMMENT '标签',
  `category` varchar(50) DEFAULT NULL COMMENT '分类',
  `view_count` int(11) NOT NULL DEFAULT '0' COMMENT '浏览量',
  `like_count` int(11) NOT NULL DEFAULT '0' COMMENT '点赞数',
  `comment_count` int(11) NOT NULL DEFAULT '0' COMMENT '评论数',
  `status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '状态：1-正常，2-隐藏，3-审核中',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_type` (`type`),
  KEY `idx_category` (`category`),
  KEY `idx_status` (`status`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_works_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作品表';
```

#### 2.4.2 动态表 (posts)
```sql
CREATE TABLE `posts` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '动态ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `content` text NOT NULL COMMENT '动态内容',
  `images` json DEFAULT NULL COMMENT '图片URL列表',
  `video_url` varchar(500) DEFAULT NULL COMMENT '视频URL',
  `location` varchar(100) DEFAULT NULL COMMENT '位置',
  `view_count` int(11) NOT NULL DEFAULT '0' COMMENT '浏览量',
  `like_count` int(11) NOT NULL DEFAULT '0' COMMENT '点赞数',
  `comment_count` int(11) NOT NULL DEFAULT '0' COMMENT '评论数',
  `share_count` int(11) NOT NULL DEFAULT '0' COMMENT '分享数',
  `status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '状态：1-正常，2-隐藏，3-审核中',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_posts_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='动态表';
```

#### 2.4.3 评论表 (comments)
```sql
CREATE TABLE `comments` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '评论ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `target_type` varchar(20) NOT NULL COMMENT '目标类型：work-作品，post-动态，booking-预约',
  `target_id` bigint(20) unsigned NOT NULL COMMENT '目标ID',
  `parent_id` bigint(20) unsigned DEFAULT NULL COMMENT '父评论ID',
  `content` text NOT NULL COMMENT '评论内容',
  `rating` tinyint(1) DEFAULT NULL COMMENT '评分：1-5星',
  `like_count` int(11) NOT NULL DEFAULT '0' COMMENT '点赞数',
  `status` tinyint(1) NOT NULL DEFAULT '1' COMMENT '状态：1-正常，2-隐藏，3-审核中',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_target` (`target_type`, `target_id`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_comments_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_comments_parent_id` FOREIGN KEY (`parent_id`) REFERENCES `comments` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';
```

### 2.5 消息通知模块

#### 2.5.1 消息表 (messages)
```sql
CREATE TABLE `messages` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '消息ID',
  `sender_id` bigint(20) unsigned DEFAULT NULL COMMENT '发送者ID',
  `receiver_id` bigint(20) unsigned NOT NULL COMMENT '接收者ID',
  `type` varchar(20) NOT NULL COMMENT '消息类型：system-系统，booking-预约，comment-评论，like-点赞',
  `title` varchar(200) NOT NULL COMMENT '消息标题',
  `content` text NOT NULL COMMENT '消息内容',
  `data` json DEFAULT NULL COMMENT '附加数据',
  `is_read` tinyint(1) NOT NULL DEFAULT '0' COMMENT '是否已读：0-未读，1-已读',
  `read_at` timestamp NULL DEFAULT NULL COMMENT '阅读时间',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_sender_id` (`sender_id`),
  KEY `idx_receiver_id` (`receiver_id`),
  KEY `idx_type` (`type`),
  KEY `idx_is_read` (`is_read`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_messages_sender_id` FOREIGN KEY (`sender_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_messages_receiver_id` FOREIGN KEY (`receiver_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='消息表';
```

### 2.6 系统管理模块

#### 2.6.1 系统配置表 (system_configs)
```sql
CREATE TABLE `system_configs` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `key` varchar(100) NOT NULL COMMENT '配置键',
  `value` text COMMENT '配置值',
  `description` varchar(500) DEFAULT NULL COMMENT '配置描述',
  `type` varchar(20) NOT NULL DEFAULT 'string' COMMENT '数据类型：string, number, boolean, json',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_key` (`key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';
```

#### 2.6.2 操作日志表 (operation_logs)
```sql
CREATE TABLE `operation_logs` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `user_id` bigint(20) unsigned DEFAULT NULL COMMENT '用户ID',
  `action` varchar(50) NOT NULL COMMENT '操作动作',
  `resource_type` varchar(50) DEFAULT NULL COMMENT '资源类型',
  `resource_id` bigint(20) unsigned DEFAULT NULL COMMENT '资源ID',
  `ip_address` varchar(45) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` varchar(500) DEFAULT NULL COMMENT '用户代理',
  `request_data` json DEFAULT NULL COMMENT '请求数据',
  `response_data` json DEFAULT NULL COMMENT '响应数据',
  `status` varchar(20) NOT NULL COMMENT '状态：success, error',
  `error_message` text COMMENT '错误信息',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_action` (`action`),
  KEY `idx_resource` (`resource_type`, `resource_id`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_operation_logs_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

## 3. 索引优化策略

### 3.1 主要索引
- **用户表**：用户名、邮箱、手机号唯一索引，用户类型、状态、地区普通索引
- **档期表**：用户ID+日期唯一索引，日期、状态、地区普通索引
- **预约表**：预约编号唯一索引，主持人ID、客户ID、婚礼日期、状态普通索引
- **作品表**：用户ID、类型、分类、创建时间普通索引
- **消息表**：接收者ID、类型、是否已读、创建时间普通索引

### 3.2 复合索引
- `idx_user_date` (user_id, date) - 档期表
- `idx_target` (target_type, target_id) - 评论表
- `idx_team_user` (team_id, user_id) - 团队成员表

## 4. 数据分区策略

### 4.1 时间分区
- **档期表**：按月分区，提高查询性能
- **消息表**：按月分区，定期清理历史数据
- **操作日志表**：按月分区，保留6个月数据

### 4.2 哈希分区
- **用户表**：按用户ID哈希分区，支持水平扩展
- **作品表**：按用户ID哈希分区，均匀分布数据

## 5. 缓存策略

### 5.1 Redis缓存
- **用户信息**：缓存30分钟，key格式：`user:{user_id}`
- **档期信息**：缓存15分钟，key格式：`schedule:{user_id}:{date}`
- **热门主持人**：缓存1小时，key格式：`hot_hosts:{location}`
- **系统配置**：缓存24小时，key格式：`config:{key}`

### 5.2 缓存更新策略
- **主动更新**：数据变更时主动删除相关缓存
- **被动更新**：缓存过期时重新加载数据
- **预热策略**：系统启动时预加载热点数据

## 6. 数据备份与恢复

### 6.1 备份策略
- **全量备份**：每日凌晨2点进行全量备份
- **增量备份**：每小时进行增量备份
- **日志备份**：实时备份binlog日志

### 6.2 恢复策略
- **时间点恢复**：支持任意时间点数据恢复
- **表级恢复**：支持单表数据恢复
- **异地备份**：备份数据存储在不同地域

## 7. 性能监控

### 7.1 监控指标
- **QPS/TPS**：每秒查询/事务数
- **响应时间**：平均响应时间和P99响应时间
- **连接数**：数据库连接数使用情况
- **慢查询**：执行时间超过1秒的查询

### 7.2 告警策略
- **连接数告警**：连接数超过80%时告警
- **响应时间告警**：P99响应时间超过500ms时告警
- **慢查询告警**：慢查询数量超过阈值时告警
- **磁盘空间告警**：磁盘使用率超过85%时告警

## 8. 数据安全

### 8.1 访问控制
- **用户权限**：不同用户角色具有不同的数据访问权限
- **IP白名单**：限制数据库访问IP地址
- **SSL连接**：使用SSL加密数据传输

### 8.2 数据加密
- **敏感数据加密**：密码、身份证号等敏感信息加密存储
- **传输加密**：使用HTTPS和SSL加密数据传输
- **备份加密**：备份文件进行加密存储

### 8.3 审计日志
- **操作审计**：记录所有数据变更操作
- **访问审计**：记录数据库访问日志
- **权限审计**：定期审计用户权限设置