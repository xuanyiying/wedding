# 婚礼主持俱乐部官方网站 - 数据库设计

## 📊 数据库概述

### 设计原则
- **规范化设计**：遵循第三范式，减少数据冗余
- **性能优化**：合理设置索引，优化查询性能
- **扩展性**：支持水平扩展和垂直扩展
- **数据完整性**：通过约束保证数据一致性
- **安全性**：敏感数据加密，访问权限控制

### 技术选型
- **主数据库**：MySQL 8.0+
- **缓存数据库**：Redis 6.0+
- **搜索引擎**：Elasticsearch 7.0+（可选）
- **文件存储**：阿里云OSS / MinIO

## 🗄️ 数据库架构

### 架构图
```
┌─────────────────────────────────────────────────────────────────┐
│                        数据库架构图                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   应用层                                │   │
│  │  Node.js应用 • 连接池管理 • ORM框架 • 事务管理         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                │                               │
│                                ▼                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   缓存层                                │   │
│  │  Redis集群 • 查询缓存 • 会话缓存 • 计数器缓存          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                │                               │
│                                ▼                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   数据库层                              │   │
│  │  MySQL主库 • MySQL从库 • 读写分离 • 主从同步           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                │                               │
│                                ▼                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   存储层                                │   │
│  │  数据文件 • 日志文件 • 备份文件 • 索引文件             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📋 数据表设计

### 用户管理模块

#### 用户表 (users)
```sql
CREATE TABLE `users` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `password_hash` varchar(255) NOT NULL COMMENT '密码哈希',
  `avatar` varchar(500) DEFAULT NULL COMMENT '头像URL',
  `real_name` varchar(50) DEFAULT NULL COMMENT '真实姓名',
  `gender` tinyint(1) DEFAULT NULL COMMENT '性别：1-男，2-女',
  `birthday` date DEFAULT NULL COMMENT '生日',
  `location` varchar(100) DEFAULT NULL COMMENT '所在地区',
  `bio` text COMMENT '个人简介',
  `role` enum('super_admin','admin','member','guest') NOT NULL DEFAULT 'member' COMMENT '用户角色',
  `status` enum('active','inactive','banned','pending') NOT NULL DEFAULT 'pending' COMMENT '用户状态',
  `verified` tinyint(1) NOT NULL DEFAULT '0' COMMENT '是否实名认证：0-否，1-是',
  `last_login_at` timestamp NULL DEFAULT NULL COMMENT '最后登录时间',
  `login_count` int(11) NOT NULL DEFAULT '0' COMMENT '登录次数',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_email` (`email`),
  UNIQUE KEY `uk_phone` (`phone`),
  KEY `idx_role` (`role`),
  KEY `idx_status` (`status`),
  KEY `idx_location` (`location`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

#### 用户档案表 (user_profiles)
```sql
CREATE TABLE `user_profiles` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '档案ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `professional_title` varchar(100) DEFAULT NULL COMMENT '职业头衔',
  `experience_years` int(11) DEFAULT NULL COMMENT '从业年限',
  `specialties` json DEFAULT NULL COMMENT '专业技能标签',
  `service_areas` json DEFAULT NULL COMMENT '服务地区',
  `price_range` json DEFAULT NULL COMMENT '价格区间',
  `introduction` text COMMENT '个人介绍',
  `achievements` json DEFAULT NULL COMMENT '个人成就',
  `certifications` json DEFAULT NULL COMMENT '资质证书',
  `social_links` json DEFAULT NULL COMMENT '社交媒体链接',
  `work_schedule` json DEFAULT NULL COMMENT '工作时间安排',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_experience_years` (`experience_years`),
  CONSTRAINT `fk_user_profiles_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户档案表';
```

### 团队管理模块

#### 团队表 (teams)
```sql
CREATE TABLE `teams` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '团队ID',
  `name` varchar(100) NOT NULL COMMENT '团队名称',
  `description` text COMMENT '团队描述',
  `logo` varchar(500) DEFAULT NULL COMMENT '团队Logo',
  `cover_image` varchar(500) DEFAULT NULL COMMENT '封面图片',
  `established_date` date DEFAULT NULL COMMENT '成立日期',
  `location` varchar(100) DEFAULT NULL COMMENT '团队所在地',
  `contact_info` json DEFAULT NULL COMMENT '联系信息',
  `social_links` json DEFAULT NULL COMMENT '社交媒体链接',
  `status` enum('active','inactive','disbanded') NOT NULL DEFAULT 'active' COMMENT '团队状态',
  `member_count` int(11) NOT NULL DEFAULT '0' COMMENT '成员数量',
  `created_by` bigint(20) unsigned NOT NULL COMMENT '创建者ID',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_name` (`name`),
  KEY `idx_location` (`location`),
  KEY `idx_status` (`status`),
  KEY `idx_created_by` (`created_by`),
  CONSTRAINT `fk_teams_created_by` FOREIGN KEY (`created_by`) REFERENCES `users` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='团队表';
```

#### 团队成员表 (team_members)
```sql
CREATE TABLE `team_members` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '成员关系ID',
  `team_id` bigint(20) unsigned NOT NULL COMMENT '团队ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `role` enum('leader','admin','member') NOT NULL DEFAULT 'member' COMMENT '团队角色',
  `position` varchar(50) DEFAULT NULL COMMENT '职位',
  `join_date` date NOT NULL COMMENT '加入日期',
  `leave_date` date DEFAULT NULL COMMENT '离开日期',
  `status` enum('active','inactive','left') NOT NULL DEFAULT 'active' COMMENT '成员状态',
  `permissions` json DEFAULT NULL COMMENT '权限设置',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_team_user` (`team_id`, `user_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_role` (`role`),
  KEY `idx_status` (`status`),
  CONSTRAINT `fk_team_members_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_team_members_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='团队成员表';
```

### 档期管理模块

#### 档期表 (schedules)
```sql
CREATE TABLE `schedules` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '档期ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `title` varchar(200) NOT NULL COMMENT '档期标题',
  `description` text COMMENT '档期描述',
  `event_date` date NOT NULL COMMENT '活动日期',
  `start_time` time NOT NULL COMMENT '开始时间',
  `end_time` time NOT NULL COMMENT '结束时间',
  `location` varchar(200) DEFAULT NULL COMMENT '活动地点',
  `event_type` varchar(50) DEFAULT NULL COMMENT '活动类型',
  `price` decimal(10,2) DEFAULT NULL COMMENT '服务价格',
  `status` enum('available','booked','confirmed','completed','cancelled') NOT NULL DEFAULT 'available' COMMENT '档期状态',
  `booking_deadline` datetime DEFAULT NULL COMMENT '预订截止时间',
  `notes` text COMMENT '备注信息',
  `metadata` json DEFAULT NULL COMMENT '扩展信息',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_event_date` (`event_date`),
  KEY `idx_status` (`status`),
  KEY `idx_event_type` (`event_type`),
  KEY `idx_location` (`location`),
  CONSTRAINT `fk_schedules_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='档期表';
```

#### 预约表 (bookings)
```sql
CREATE TABLE `bookings` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '预约ID',
  `schedule_id` bigint(20) unsigned NOT NULL COMMENT '档期ID',
  `customer_name` varchar(100) NOT NULL COMMENT '客户姓名',
  `customer_phone` varchar(20) NOT NULL COMMENT '客户电话',
  `customer_email` varchar(100) DEFAULT NULL COMMENT '客户邮箱',
  `event_details` json DEFAULT NULL COMMENT '活动详情',
  `special_requirements` text COMMENT '特殊要求',
  `booking_amount` decimal(10,2) NOT NULL COMMENT '预约金额',
  `payment_status` enum('pending','paid','partial','refunded') NOT NULL DEFAULT 'pending' COMMENT '支付状态',
  `booking_status` enum('pending','confirmed','cancelled','completed') NOT NULL DEFAULT 'pending' COMMENT '预约状态',
  `booking_source` varchar(50) DEFAULT NULL COMMENT '预约来源',
  `confirmation_code` varchar(32) DEFAULT NULL COMMENT '确认码',
  `cancelled_reason` text COMMENT '取消原因',
  `cancelled_at` timestamp NULL DEFAULT NULL COMMENT '取消时间',
  `confirmed_at` timestamp NULL DEFAULT NULL COMMENT '确认时间',
  `completed_at` timestamp NULL DEFAULT NULL COMMENT '完成时间',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_confirmation_code` (`confirmation_code`),
  KEY `idx_schedule_id` (`schedule_id`),
  KEY `idx_customer_phone` (`customer_phone`),
  KEY `idx_booking_status` (`booking_status`),
  KEY `idx_payment_status` (`payment_status`),
  CONSTRAINT `fk_bookings_schedule_id` FOREIGN KEY (`schedule_id`) REFERENCES `schedules` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='预约表';
```

### 内容管理模块

#### 作品表 (works)
```sql
CREATE TABLE `works` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '作品ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `title` varchar(200) NOT NULL COMMENT '作品标题',
  `description` text COMMENT '作品描述',
  `category` varchar(50) NOT NULL COMMENT '作品分类',
  `tags` json DEFAULT NULL COMMENT '标签',
  `cover_image` varchar(500) DEFAULT NULL COMMENT '封面图片',
  `media_files` json DEFAULT NULL COMMENT '媒体文件',
  `event_date` date DEFAULT NULL COMMENT '活动日期',
  `location` varchar(200) DEFAULT NULL COMMENT '拍摄地点',
  `client_info` json DEFAULT NULL COMMENT '客户信息（脱敏）',
  `view_count` int(11) NOT NULL DEFAULT '0' COMMENT '浏览次数',
  `like_count` int(11) NOT NULL DEFAULT '0' COMMENT '点赞次数',
  `comment_count` int(11) NOT NULL DEFAULT '0' COMMENT '评论次数',
  `share_count` int(11) NOT NULL DEFAULT '0' COMMENT '分享次数',
  `status` enum('draft','published','archived','deleted') NOT NULL DEFAULT 'draft' COMMENT '发布状态',
  `featured` tinyint(1) NOT NULL DEFAULT '0' COMMENT '是否精选',
  `sort_order` int(11) NOT NULL DEFAULT '0' COMMENT '排序权重',
  `published_at` timestamp NULL DEFAULT NULL COMMENT '发布时间',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_category` (`category`),
  KEY `idx_status` (`status`),
  KEY `idx_featured` (`featured`),
  KEY `idx_published_at` (`published_at`),
  KEY `idx_sort_order` (`sort_order`),
  CONSTRAINT `fk_works_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='作品表';
```

#### 评论表 (comments)
```sql
CREATE TABLE `comments` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '评论ID',
  `work_id` bigint(20) unsigned NOT NULL COMMENT '作品ID',
  `user_id` bigint(20) unsigned DEFAULT NULL COMMENT '用户ID',
  `parent_id` bigint(20) unsigned DEFAULT NULL COMMENT '父评论ID',
  `content` text NOT NULL COMMENT '评论内容',
  `author_name` varchar(50) DEFAULT NULL COMMENT '评论者姓名',
  `author_email` varchar(100) DEFAULT NULL COMMENT '评论者邮箱',
  `author_ip` varchar(45) DEFAULT NULL COMMENT '评论者IP',
  `like_count` int(11) NOT NULL DEFAULT '0' COMMENT '点赞次数',
  `status` enum('pending','approved','rejected','spam') NOT NULL DEFAULT 'pending' COMMENT '审核状态',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_work_id` (`work_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_status` (`status`),
  KEY `idx_created_at` (`created_at`),
  CONSTRAINT `fk_comments_work_id` FOREIGN KEY (`work_id`) REFERENCES `works` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_comments_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_comments_parent_id` FOREIGN KEY (`parent_id`) REFERENCES `comments` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='评论表';
```

### 系统管理模块

#### 系统配置表 (system_configs)
```sql
CREATE TABLE `system_configs` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '配置ID',
  `config_key` varchar(100) NOT NULL COMMENT '配置键',
  `config_value` text COMMENT '配置值',
  `config_type` enum('string','number','boolean','json','text') NOT NULL DEFAULT 'string' COMMENT '配置类型',
  `category` varchar(50) NOT NULL COMMENT '配置分类',
  `description` varchar(255) DEFAULT NULL COMMENT '配置描述',
  `is_public` tinyint(1) NOT NULL DEFAULT '0' COMMENT '是否公开',
  `sort_order` int(11) NOT NULL DEFAULT '0' COMMENT '排序权重',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_config_key` (`config_key`),
  KEY `idx_category` (`category`),
  KEY `idx_is_public` (`is_public`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统配置表';
```

#### 权限表 (permissions)
```sql
CREATE TABLE `permissions` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '权限ID',
  `name` varchar(100) NOT NULL COMMENT '权限名称',
  `code` varchar(100) NOT NULL COMMENT '权限代码',
  `description` varchar(255) DEFAULT NULL COMMENT '权限描述',
  `category` varchar(50) NOT NULL COMMENT '权限分类',
  `resource` varchar(100) DEFAULT NULL COMMENT '资源标识',
  `action` varchar(50) DEFAULT NULL COMMENT '操作类型',
  `sort_order` int(11) NOT NULL DEFAULT '0' COMMENT '排序权重',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`),
  KEY `idx_category` (`category`),
  KEY `idx_resource` (`resource`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='权限表';
```

#### 用户权限表 (user_permissions)
```sql
CREATE TABLE `user_permissions` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '用户权限ID',
  `user_id` bigint(20) unsigned NOT NULL COMMENT '用户ID',
  `permission_id` bigint(20) unsigned NOT NULL COMMENT '权限ID',
  `granted_by` bigint(20) unsigned DEFAULT NULL COMMENT '授权人ID',
  `granted_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '授权时间',
  `expires_at` timestamp NULL DEFAULT NULL COMMENT '过期时间',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_permission` (`user_id`, `permission_id`),
  KEY `idx_permission_id` (`permission_id`),
  KEY `idx_granted_by` (`granted_by`),
  CONSTRAINT `fk_user_permissions_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_user_permissions_permission_id` FOREIGN KEY (`permission_id`) REFERENCES `permissions` (`id`) ON DELETE CASCADE,
  CONSTRAINT `fk_user_permissions_granted_by` FOREIGN KEY (`granted_by`) REFERENCES `users` (`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户权限表';
```

## 📊 数据关系图

### ER图
```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    users    │──────▶│user_profiles│       │    teams    │
│             │       │             │       │             │
│ id (PK)     │       │ user_id(FK) │       │ id (PK)     │
│ username    │       │ title       │       │ name        │
│ email       │       │ experience  │       │ description │
│ role        │       │ specialties │       │ status      │
│ status      │       │             │       │             │
└─────────────┘       └─────────────┘       └─────────────┘
       │                                           │
       │              ┌─────────────┐              │
       └─────────────▶│team_members │◀─────────────┘
                      │             │
                      │ team_id(FK) │
                      │ user_id(FK) │
                      │ role        │
                      │ status      │
                      └─────────────┘
       │
       │              ┌─────────────┐
       └─────────────▶│  schedules  │
                      │             │
                      │ user_id(FK) │
                      │ event_date  │
                      │ status      │
                      │ price       │
                      └─────────────┘
                             │
                             │
                      ┌─────────────┐
                      │  bookings   │
                      │             │
                      │schedule_id  │
                      │customer_name│
                      │booking_status│
                      │payment_status│
                      └─────────────┘

┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    works    │──────▶│  comments   │       │permissions  │
│             │       │             │       │             │
│ id (PK)     │       │ work_id(FK) │       │ id (PK)     │
│ user_id(FK) │       │ user_id(FK) │       │ code        │
│ title       │       │ content     │       │ name        │
│ category    │       │ status      │       │ category    │
│ status      │       │             │       │             │
└─────────────┘       └─────────────┘       └─────────────┘
                                                   │
                                                   │
                                            ┌─────────────┐
                                            │user_perms   │
                                            │             │
                                            │user_id(FK)  │
                                            │permission_id│
                                            │granted_at   │
                                            └─────────────┘
```

## 🔍 索引策略

### 主要索引
```sql
-- 用户表索引
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_phone ON users(phone);
CREATE INDEX idx_users_role_status ON users(role, status);
CREATE INDEX idx_users_created_at ON users(created_at);

-- 档期表索引
CREATE INDEX idx_schedules_user_date ON schedules(user_id, event_date);
CREATE INDEX idx_schedules_status_date ON schedules(status, event_date);
CREATE INDEX idx_schedules_location ON schedules(location);
CREATE INDEX idx_schedules_event_type ON schedules(event_type);

-- 作品表索引
CREATE INDEX idx_works_user_status ON works(user_id, status);
CREATE INDEX idx_works_category_published ON works(category, published_at);
CREATE INDEX idx_works_featured_sort ON works(featured, sort_order);

-- 预约表索引
CREATE INDEX idx_bookings_schedule_status ON bookings(schedule_id, booking_status);
CREATE INDEX idx_bookings_customer_phone ON bookings(customer_phone);
CREATE INDEX idx_bookings_created_at ON bookings(created_at);

-- 评论表索引
CREATE INDEX idx_comments_work_status ON comments(work_id, status);
CREATE INDEX idx_comments_user_created ON comments(user_id, created_at);
```

### 复合索引优化
```sql
-- 档期查询优化
CREATE INDEX idx_schedules_query ON schedules(status, event_date, location, event_type);

-- 作品展示优化
CREATE INDEX idx_works_display ON works(status, featured, category, published_at);

-- 用户权限查询优化
CREATE INDEX idx_user_permissions_query ON user_permissions(user_id, expires_at);
```

## 💾 数据备份策略

### 备份方案
```sql
-- 全量备份（每日）
mysqldump --single-transaction --routines --triggers \
  --all-databases > backup_full_$(date +%Y%m%d).sql

-- 增量备份（每小时）
mysqlbinlog --start-datetime="2025-09-16 00:00:00" \
  --stop-datetime="2025-09-16 01:00:00" \
  mysql-bin.000001 > backup_inc_$(date +%Y%m%d_%H).sql

-- 数据验证
mysql -e "SELECT COUNT(*) FROM users;" wedding_db
mysql -e "SELECT COUNT(*) FROM schedules;" wedding_db
mysql -e "SELECT COUNT(*) FROM works;" wedding_db
```

### 恢复策略
```sql
-- 全量恢复
mysql wedding_db < backup_full_20250916.sql

-- 增量恢复
mysql wedding_db < backup_inc_20250916_01.sql
mysql wedding_db < backup_inc_20250916_02.sql

-- 数据一致性检查
CHECK TABLE users;
CHECK TABLE schedules;
CHECK TABLE works;
```

## 🔧 性能优化

### 查询优化
```sql
-- 分页查询优化
SELECT * FROM works 
WHERE status = 'published' 
ORDER BY published_at DESC 
LIMIT 20 OFFSET 0;

-- 统计查询优化
SELECT 
  DATE(created_at) as date,
  COUNT(*) as count
FROM bookings 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY DATE(created_at);

-- 关联查询优化
SELECT 
  u.username,
  up.professional_title,
  COUNT(w.id) as work_count
FROM users u
LEFT JOIN user_profiles up ON u.id = up.user_id
LEFT JOIN works w ON u.id = w.user_id AND w.status = 'published'
WHERE u.status = 'active'
GROUP BY u.id;
```

### 分区策略
```sql
-- 按日期分区（档期表）
ALTER TABLE schedules PARTITION BY RANGE (YEAR(event_date)) (
  PARTITION p2024 VALUES LESS THAN (2025),
  PARTITION p2025 VALUES LESS THAN (2026),
  PARTITION p2026 VALUES LESS THAN (2027),
  PARTITION p_future VALUES LESS THAN MAXVALUE
);

-- 按哈希分区（日志表）
CREATE TABLE operation_logs (
  id BIGINT AUTO_INCREMENT,
  user_id BIGINT,
  action VARCHAR(100),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id, user_id)
) PARTITION BY HASH(user_id) PARTITIONS 8;
```

---

*文档版本：v1.0*  
*最后更新：2025-09-16*  
*维护人员：数据库团队*