# =============================================================================
# 开发环境配置 (Development Environment)
# =============================================================================
# 创建时间: 2025-09-01
# 维护者: DevOps Team
# 描述: 本地开发环境配置，支持热重载和调试功能
# =============================================================================

# 环境标识
NODE_ENV=development
ENVIRONMENT=dev
DEPLOY_ENV=development

# 服务器配置
SERVER_HOST=localhost
SERVER_PORT=3001
WEB_PORT=3000
API_BASE_URL=http://localhost:3001

# 数据库配置
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=wedding_db_dev
MYSQL_USER=wedding_dev
MYSQL_PASSWORD=dev_password_2025
MYSQL_ROOT_PASSWORD=root_dev_2025

# Redis配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_dev_2025

# MinIO配置
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=dev_minio_access
MINIO_SECRET_KEY=dev_minio_secret_2025
MINIO_BUCKET=wedding-dev
MINIO_PUBLIC_ENDPOINT=http://localhost:9000

# 安全配置
JWT_SECRET=dev_jwt_secret_key_2025_wedding
JWT_EXPIRES_IN=24h
CORS_ORIGIN=http://localhost:3000

# 邮件配置 (开发环境使用测试邮箱)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@ethereal.email
SMTP_PASS=test_password
SMTP_FROM="Wedding Dev <noreply@wedding-dev.local>"

# 文件上传配置
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,pdf,doc,docx

# 日志配置
LOG_LEVEL=debug
LOG_FILE_ENABLED=true
LOG_CONSOLE_ENABLED=true

# 开发工具配置
HOT_RELOAD=true
DEBUG_MODE=true
MOCK_DATA=true

# Docker网络配置
DOCKER_NETWORK=wedding-dev-network
DOCKER_SUBNET=172.25.0.0/16

# API配置
API_TIMEOUT=30000
API_RATE_LIMIT=1000

# MinIO API配置
OSS_TYPE=minio
OSS_ENDPOINT=http://minio:9000
OSS_ACCESS_KEY=dev_minio_access
OSS_SECRET_KEY=dev_minio_secret_2025
OSS_BUCKET=wedding-dev

# Vite配置
VITE_API_URL=http://localhost:3001
VITE_UPLOAD_URL=http://localhost:3001/upload
VITE_MINIO_URL=http://localhost:9000
VITE_APP_TITLE="Wedding Client Dev"
VITE_APP_VERSION=1.0.0-dev

# 会话配置
SESSION_SECRET=dev_session_secret_2025
SESSION_MAX_AGE=86400000

# 缓存配置
CACHE_TTL=300
CACHE_MAX_SIZE=100
CACHE_ENABLED=true